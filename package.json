{
  "name": "maritime-reporting",
  "version": "1.0.0",
  "description": "Maritime Reporting Application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.1"
  }
}
